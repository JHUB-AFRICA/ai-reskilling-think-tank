# Copy to .env.local and adjust if your FastAPI service runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Project Settings -> Data API -> Project URL and anon/public key.
# Used by lib/supabase.ts for client-side sign-in/sign-up only --
# all actual data access still goes through the FastAPI service above,
# which verifies the resulting token server-side (app_api/auth.py).
NEXT_PUBLIC_SUPABASE_URL=https://enxescazwbobjylrsauj.supabase.co

NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_t5EfpqD5fNf59WqOkFKMJg_73vql_ld


